name: helpwave
repository: https://github.com/helpwave/mobile-app
packages:
  - packages/**
  - apps/**

scripts:
  analyze:
    run: melos exec -- "dart analyze ."

  intl:
    run: melos exec --file-exists="l10n.yaml" -- "flutter gen-l10n && melos bootstrap"

  build-apk:
    run: melos exec --file-exists="./lib/main.dart" "melos run intl && flutter build apk"

  build-ios:
    run: melos exec --file-exists="./lib/main.dart" "melos run intl && flutter build ios --no-codesign"
