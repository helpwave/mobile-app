import 'package:flutter/material.dart';

class AssigneeSelect extends StatelessWidget {
  const AssigneeSelect({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
